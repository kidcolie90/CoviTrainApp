//document.addEventListener("turbolinks:load", function(){ //once eveent is triggered by turbolinks load, function runs
                          
  //              var form = document.getElementById("save_views"); //will select the form and detect if present

//if (form !== null) form.submit(); //of present form submits and makes request to controller//
                          
                          
   // }) //js function to save the view 